pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/24p1245-ssk/Weather-App.git'
            }
        }

        stage('Done') {
            steps {
                echo 'Build completed'
            }
        }
    }
}
