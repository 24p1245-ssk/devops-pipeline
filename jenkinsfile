pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'hello world'
            }
        }

        stage('Done') {
            steps {
                echo 'Build completed'
            }
        }
    }
}
